#-*-mode:shell-script-*-
#!/bin/bash

set -e

# Not really using these yet:
PROJECT=$1

sudo apt-get install -y emacs24-nox curl tree unzip zip mosh

cd $(dirname $0)


cp .bashrc ~
cp .zshrc ~
cp .gitconfig ~
cp .tmux.conf ~
